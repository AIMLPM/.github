# What the badge on this profile means

The badge is generated from GitHub's own records, not typed in, and it is read for this account exactly the way [Unlock Radar](https://velocity.id8.one) reads any other public GitHub profile: no private access, nothing entered by hand.

**Contributions per active day, GitHub calendar.** GitHub's contribution calendar for the trailing 365 days, private repositories included via the "include private contributions" setting, divided by the number of days with any activity. The same totals are visible on the public profile.

**Public commits per active day.** Commits GitHub attributes to this account on its public repositories, divided by active days. It is a floor: most of this account's work is in private repositories, which the API does not expose to anyone, so the figure carries a ≥.

**Peak day.** The raw calendar maximum for the year.

**Momentum.** The mean of the last 30 active days against a baseline built from earlier 30-active-day windows.

**Why it is credible.** Nothing here was entered by hand, and anyone can reproduce every figure from the public profile and the public repositories with the same script.

Regenerated monthly by the same pipeline that produces the Unlock Radar, a screen over well-known builders' public GitHub activity. Method: https://velocity.id8.one/AIMLPM
